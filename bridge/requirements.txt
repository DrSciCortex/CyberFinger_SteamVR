# CyberFinger Bridge dependencies

# BLE (WinRT)
winrt-runtime
winrt-Windows.Devices.Bluetooth
winrt-Windows.Devices.Bluetooth.GenericAttributeProfile
winrt-Windows.Devices.Enumeration
winrt-Windows.Storage.Streams
winrt-Windows.Foundation
winrt-Windows.Foundation.Collections

# System tray icon
pystray
pillow

# Gamepad emulation (optional — VR mode works without it)
vgamepad
# for vrchat osc mode
python-osc

#for generating keyboard events
pynput

# Hand skeleton display (optional) — reads the skeleton SteamVR is tracking,
# e.g. Steam Link hand tracking, via OpenVR skeletal input
openvr

# Build tools (only needed for creating the .exe)
# pyinstaller
